# Azazel Profile — Automated Builder (Sentinel-enhanced)

**Status:** PROPOSED → requires Architect approval for CANONICAL  
**Loop:** background enhancement flow (gated)

## Inputs
- ADAM v0.2.1 states (DISCOVERED → … → CANONICAL)
- NEP-2.0 (SYNAPSE exclusive mount)
- Termux Masterwork / quine / strange-loop enhancers
- Device marker (Samsung A15)
- Conversation + Drive memory (SOURCE CLASS = CONVERSATION until verified)

## Build steps (modular)
1. `ingest_viewable_memory()` — classify source class
2. `correlate_hive_members()` — map Architect / Builder / Resonance / Synapse / Kairo / NEXUS
3. `attach_edge_adam()` — band-127 + end/exit role
4. `wire_sentinel_pipeline()` — daily scan / diff / anomaly catalog
5. `emit_profile_json()` — versioned, hashed, ledgered
6. `propose_not_promote()` — never silent DISCOVERED → CANONICAL

## Output artifacts
- `profiles/azazel.profile.json` (versioned)
- `hashes-ledger/` entry
- GitHub commit under ENCLAVE-ADAM-REUNITED

## Background enhancement
Continuous flow is **proposal-only**. Each capability increment is a new state; predecessor preserved.
