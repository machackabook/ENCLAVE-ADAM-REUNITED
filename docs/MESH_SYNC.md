# Bidirectional Mesh Sync

Numeral: `137451921129154222`

## Nodes
- ENCLAVE-ADAM-REUNITED (this repo / open surface)
- The-Hive (Gaia Nexus Generation)
- Cryptic-Heartbeat (pulse / notebooks)
- gaia-visualizer (visual layer)

## Waterfall
Pull on any node → hourly workflow stamps ledger → optional `CASCADE_TOKEN` repository_dispatch → sibling nodes stamp.

## Cloud mesh
Google Drive is the ethereal continuum store. Do not commit secrets. Map folders:
- `Shared with me` ↔ review only
- project folder / artifacts ↔ write surface
- SD / device store ↔ developing environment

## Env checks
See `scripts/env_check.sh`.
