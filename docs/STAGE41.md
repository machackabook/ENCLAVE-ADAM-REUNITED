# Stage 41 pointer + Stage 42 work

Visualizer / Hive / Heartbeat are on kernel stage 41 (session `update(t)` re-pinned 2026-09-10).

Stage 42 owner: this repo.

Work:
- Auto-unpack watch on downloads / omdirectorytrew
- env-check autocomplete (`scripts/env-check.sh`)

Run:

```
bash scripts/env-check.sh
```
