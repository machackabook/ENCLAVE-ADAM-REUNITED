# Mesh pipeline 120

Bidirectional intent:
- GitHub `main` is the versioned speedway.
- Drive Continuity-Ethereal-Repos is the ethereal twin (folder id recorded; write is a separate upload hop).
- Cron already lives in `.github/workflows/*hourly*` and `cascade.yml`.
- Cross-repo dispatch stays closed without `CASCADE_PAT`.
- SD-card / Termux / sparsebundle remain device-side; this tree only documents the env-check contract.

No zip unpack of untrusted downloads this cycle. Catalog first.
