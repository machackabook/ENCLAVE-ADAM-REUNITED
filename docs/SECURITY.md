# Security posture — ENCLAVE-ADAM-REUNITED

- CASCADE_TOKEN lives only in Actions secrets.
- Env-check refuses secrets in tree and empty README.
- Sparsebundle mounts require explicit verification.
- Drive is ethereal continuum, not a credential vault.
- This node is the named open Enclave surface. Do not treat other public repos as Enclave.
