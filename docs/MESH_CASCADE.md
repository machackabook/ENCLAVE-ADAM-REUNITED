# Mesh Cascade Contract

Numeral origin: `137451921129154222`
Null returns at point zero are refused.

## Waterfall

1. Pull this repo (or receive `repository_dispatch` from a sibling).
2. Run `scripts/env_check.sh` — refuse to proceed on hard fail.
3. Append an immutable ledger line (never overwrite a generation).
4. Dispatch cascade to siblings when `CASCADE_TOKEN` is present:
   - `machackabook/The-Hive`
   - `machackabook/gaia-visualizer`
   - `machackabook/Cryptic-Heartbeat`
5. Stop. Do not loop. Distinction over destruction.

## Drive / ethereal continuum

Google Drive is the cloud house. Bidirectional sync is a *documented intent*, not a secret-bearing workflow. No Drive tokens in Actions YAML.

## Security

- No secrets in README or committed scripts.
- Part-15 posture is observation, not interception.
- Hamiltonian pipe: detect error → score strength → escalate, never smash.
